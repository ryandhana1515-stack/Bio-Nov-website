# fal.ai — server-side only. Never expose in NEXT_PUBLIC_* variables.
# Used by scripts/generate-assets.ts and app/api/generate-assets.
FAL_KEY=

# Optional: token required by POST /api/generate-assets outside development.
ASSET_ADMIN_TOKEN=
